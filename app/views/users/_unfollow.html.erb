
<%= form_for(current_user.followships.find_by_followed_id(@user), html: { method: :delete }) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "Unfollow", class: "btn btn-large btn-primary btn_unfollow"%>
<% end %>